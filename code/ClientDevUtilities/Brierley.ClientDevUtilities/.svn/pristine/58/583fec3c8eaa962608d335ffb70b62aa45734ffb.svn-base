using System;
using System.Collections.Generic;
using System.Diagnostics;

//using Microsoft.Practices.EnterpriseLibrary.Logging;
//using Microsoft.Practices.EnterpriseLibrary.Logging.Filters;

namespace Brierley.FrameWork.Common.Logging
{
	//[System.Diagnostics.DebuggerStepThrough]
	//internal class LWLogFilter : ILogFilter
	//{
	//    private IDictionary<string, SourceLevels> _map = new Dictionary<string, SourceLevels>(); 

	//    public bool Filter(LogEntry logEntry)
	//    {
	//        SourceLevels logLevel = SourceLevels.All;
	//        foreach (string applicationName in logEntry.CategoriesStrings)
	//        {
	//            if (_map.ContainsKey(applicationName))
	//            {
	//                logLevel = _map[applicationName];
	//                break;
	//            }
	//        }
	//        bool result = ShouldLog(logEntry.Severity, logLevel);
	//        return result;
	//    }

	//    private bool ShouldLog(TraceEventType eventLevel, SourceLevels logLevel)
	//    {
	//        bool result = true;
	//        switch (logLevel)
	//        {
	//            case SourceLevels.Off:
	//                result = false;
	//                break;
	//            case SourceLevels.Critical:
	//                result = (eventLevel <= TraceEventType.Critical);
	//                break;
	//            case SourceLevels.Error:
	//                result = (eventLevel <= TraceEventType.Error);
	//                break;
	//            case SourceLevels.Warning:
	//                result = (eventLevel <= TraceEventType.Warning);
	//                break;
	//            case SourceLevels.Information:
	//                result = (eventLevel <= TraceEventType.Information);
	//                break;
	//            case SourceLevels.Verbose:
	//                result = (eventLevel <= TraceEventType.Verbose);
	//                break;
	//            case SourceLevels.ActivityTracing:
	//                result = (eventLevel >= TraceEventType.Start);
	//                break;
	//            case SourceLevels.All:
	//                result = true;
	//                break;
	//        }
	//        return result;
	//    }

	//    public string Name
	//    {
	//        get { return "LWLogFilter"; }
	//    }

	//    public SourceLevels GetLogLevel(string applicationName) 
	//    {
	//        SourceLevels result = SourceLevels.All;
	//        if (_map.ContainsKey(applicationName))
	//            result = _map[applicationName];
	//        return result;
	//    }

	//    public void SetLogLevel(string applicationName, SourceLevels logLevel)
	//    {
	//        if (_map.ContainsKey(applicationName))
	//        {
	//            _map[applicationName] = logLevel;
	//        }
	//        else
	//        {
	//            _map.Add(new KeyValuePair<string, SourceLevels>(applicationName, logLevel));
	//        }
	//    }
	//}
}
